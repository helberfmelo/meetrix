## Objetivo do PR
Expandir a validacao visual E2E com Playwright para landing, onboarding por modo e modulos SaaS ja entregues.

## Etapa / Escopo
- Plano: E2E visual expandido
- Itens implementados:
  - Configuracao do Playwright com projetos desktop e mobile.
  - Testes visuais para Home, Login e Onboarding por modo.
  - Teste condicional de modulos SaaS (Dashboard, Account, Master Admin) com credenciais via env.
  - Capturas salvas em `artifacts/e2e-visual/`.

## Fora de escopo (neste PR)
- Comparacao automatica de baselines (visual diff).
- Execucao em CI.

## Mudancas tecnicas
- Testes:
  - `playwright.config.mjs`
  - `tests/e2e/visual.spec.mjs`
- Scripts:
  - `npm run test:e2e`

## Arquivos principais alterados
- `playwright.config.mjs`
- `tests/e2e/visual.spec.mjs`
- `package.json`
- `package-lock.json`
- `docs/PR_E2E_2026-02-22.md`

## Evidencias de teste
- [ ] `npx playwright install` executado (primeira vez)
- [ ] `npm run test:e2e` executado
- [ ] Evidencias em `artifacts/e2e-visual/`

Comandos executados:
```bash
npx playwright install
npm run test:e2e
```

Resultado:
- <pendente>

## Gate
- [ ] Testes E2E visuais executados em desktop e mobile
- [ ] Evidencias armazenadas em `artifacts/e2e-visual/`

## Observacoes
- Para validar modulos SaaS, definir `E2E_ADMIN_EMAIL` e `E2E_ADMIN_PASSWORD`.
- Para rodar em outro ambiente, definir `E2E_BASE_URL`.
